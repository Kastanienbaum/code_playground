#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
	for (int i = 0; i < argc; ++i)
	{
		printf("arg[%d]: %s\n", i, argv[i]);
		//typedef int interesting_##varname = 0; 
		printf("%s\n", sizeOfuint8);
	}
	return 0;
}